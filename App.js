let heading = React.createElement("h1" , {} , "Ram Ram Bhai Sariyane");
let root = ReactDOM.createRoot(document.getElementById("root"));
/* Note :- root and heading are react elements which are objects at the end of the 
        and not a so called html elements */
root.render(heading);